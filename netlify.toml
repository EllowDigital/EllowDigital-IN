[build]
  publish = "dist"
  # Use npm install for better compatibility with lovable-tagger and optional dependencies
  command = "npm install && npm run build"

[build.environment]
  # Pin to a stable LTS; Netlify log shows Node 22 which can be coupled with npm optional-deps issues.
  NODE_VERSION = "20"
